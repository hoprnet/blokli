Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }}.

To learn more about the release, try:

  $ helm status {{ .Release.Name }} -n {{ .Release.Namespace }}
  $ helm get all {{ .Release.Name }} -n {{ .Release.Namespace }}

{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
  {{- range .paths }}

The Blokli GraphQL API is available at:

  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}

  {{- end }}
{{- end }}
{{- else if contains "NodePort" .Values.service.type }}

Get the application URL by running these commands:

  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "blokli.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT

{{- else if contains "LoadBalancer" .Values.service.type }}

Get the application URL by running these commands:

  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
        You can watch the status by running 'kubectl get --namespace {{ .Release.Namespace }} svc -w {{ include "blokli.fullname" . }}'

  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "blokli.fullname" . }} --template "{{"{{ range (index .status.loadBalancer.ingress 0) }}{{.}}{{ end }}"}}")
  echo http://$SERVICE_IP:{{ .Values.service.port }}

{{- else if contains "ClusterIP" .Values.service.type }}

Get the application URL by running these commands:

  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "blokli.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -o jsonpath="{.items[0].metadata.name}")
  export CONTAINER_PORT=$(kubectl get pod --namespace {{ .Release.Namespace }} $POD_NAME -o jsonpath="{.spec.containers[0].ports[0].containerPort}")
  echo "Visit http://127.0.0.1:{{ .Values.service.port }} to access the GraphQL API"
  kubectl --namespace {{ .Release.Namespace }} port-forward $POD_NAME {{ .Values.service.port }}:$CONTAINER_PORT

{{- end }}

API Endpoints:
  - GraphQL API: /graphql
  - GraphQL Playground: /graphql (GET request, if enabled)
  - Liveness Probe: /healthz
  - Readiness Probe: /readyz

Note: GraphQL subscriptions use Server-Sent Events (SSE) via the /graphql endpoint
with Accept: text/event-stream header.

Configuration:
  - Network: {{ .Values.config.network }}
  - RPC URL: {{ .Values.config.rpcUrl }}
  - Database: {{ .Values.database.host }}:{{ .Values.database.port }}/{{ .Values.database.database }}
  - Data Directory: {{ .Values.config.dataDirectory }}

{{- if .Values.persistence.enabled }}

Persistent Volume:
  - Storage: {{ .Values.persistence.size }}
  {{- if .Values.persistence.storageClass }}
  - Storage Class: {{ .Values.persistence.storageClass }}
  {{- end }}
{{- else }}

WARNING: Persistence is disabled. Data will be lost on pod restart!
{{- end }}

For more information about Blokli, visit:
  https://github.com/hoprnet/hopr-blokli
